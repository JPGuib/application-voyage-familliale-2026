# Story 25.2: Sondage destination pre-deblocage + verrouillage + scoring bonifie

Status: ready-for-dev

## Story

As a family member,
I want to submit up to 3 destination predictions before unlock and then see final results after unlock,
so that we keep suspense before departure and reward accurate, early guesses.

## Story Requirements

### Business Goal

Ajouter un ecran de sondage destination sur le parcours pre-depart (story 25.1) avec:
- Saisie libre de 1 a 3 propositions par profil avant deblocage.
- Blocage total des modifications apres deblocage.
- Affichage des resultats sur le meme ecran une fois debloque.
- Attribution de points bonifies selon exactitude et ordre.

### Functional Rules (source user request consolidated)

1. Le sondage est disponible tant que phase = before.
2. Chaque profil peut saisir jusqu'a 3 propositions, pas plus.
3. Tant que phase = before, le profil peut modifier librement ses propres propositions.
4. Des que phase = during, toutes les propositions sont figees (aucune edition).
5. L'ecran resultat (meme ecran transforme) affiche qui a vote quoi.
6. Regle de points pour un profil eligibile:
	 - 20 points si au moins une proposition correspond exactement a la destination reelle.
	 - Bonus +10 si le profil est classe 1er parmi les bonnes reponses.
	 - Bonus +5 si le profil est classe 2eme parmi les bonnes reponses.
7. Les comparaisons de texte sont insensibles a la casse (maj/min).
8. Le score du sondage ne concerne que les profils role = utilisateur pour le total partage (coherence stories 19.2 et 24.3). Les profils proprietaire/visiteur peuvent participer au jeu d'affichage mais ne gagnent pas de points partages.
9. L'ecran de saisie doit expliquer explicitement la bonification (gamification): 20 base, +10 premier, +5 deuxieme.

### Text Matching Contract (exact word with case-insensitive behavior)

Le mot doit etre exact sur le fond semantique, mais robuste aux variations de saisie usuelles:
- trim debut/fin,
- normalisation Unicode,
- comparaison insensible a la casse.

Implementation directive:
- Utiliser une normalisation centralisee (fonction utilitaire pure) appliquee des deux cotes (saisie utilisateur et destination de reference).
- Recommandation: String.normalize("NFC") + localeCompare("fr", { sensitivity: "base" }) pour eliminer les faux-negatifs de casse/accents de composition Unicode.

## Acceptance Criteria

1. Avant deblocage, chaque profil peut enregistrer entre 1 et 3 propositions; au-dela de 3, la validation est refusee avec message explicite.
2. Avant deblocage, le meme profil peut modifier ses propositions sans limitation de nombre de modifications.
3. Apres bascule phase = during, les champs de saisie et actions de validation sont desactives pour tous les profils.
4. Apres deblocage, le meme ecran affiche un mode resultat listant au minimum: profil, propositions saisies, reponse correcte/ou non, points obtenus.
5. Le calcul de points respecte exactement: 20 base bonne reponse, +10 rang 1, +5 rang 2, 0 si faux.
6. Les egalites de timestamp sur une bonne reponse partagent le meme rang et le meme bonus, sans crash ni ordre instable.
7. Les roles proprietaire et visiteur peuvent apparaitre dans l'affichage des reponses, mais n'ajoutent pas de points au classement partage.
8. La comparaison destination attendue vs propositions est insensible a la casse et ne produit pas de faux negatif entre formes Unicode equivalentes.
9. Le texte UX de l'ecran pre-vote explique la bonification en termes simples.

## Tasks / Subtasks

- [ ] Concevoir le modele de donnees cloud du sondage (AC: 1,2,3,4,5,6,7,8)
	- [ ] Ajouter types cloud dedies au sondage destination.
	- [ ] Integrer parsing cloud tolerant aux anciens snapshots (backward compatible).
- [ ] Implementer la logique de normalisation et scoring (AC: 5,6,8)
	- [ ] Creer utilitaire pur de normalisation de destination.
	- [ ] Creer utilitaire pur computeDestinationSurveyResults.
	- [ ] Ajouter tests unitaires exhaustifs (casse, Unicode, egalite de rang, roles exclus score).
- [ ] Integrer ecran App pre/post deblocage (AC: 1,2,3,4,9)
	- [ ] Ajouter navigation vers ecran teaser/sondage selon conventions existantes.
	- [ ] Mode before: formulaire 1..3 propositions editable.
	- [ ] Mode during: ecran resultat en lecture seule (meme ecran transforme).
	- [ ] Afficher regles de bonification de facon visible.
- [ ] Integrer persistance cloud + securite (AC: 1,2,3,4,7)
	- [ ] Ecriture author-only du vote profil.
	- [ ] Lecture family-wide des resultats.
	- [ ] Adapter tests de rules RTDB.
- [ ] Verifier non-regression navigation/phase/cloud (AC: 3,4)
	- [ ] Integration tests App phase before/during.
	- [ ] Validation manuelle multi-profils.

## Developer Context

### Technical Requirements

- Respecter architecture cloud-authoritative deja en place:
	- source de verite partagee via snapshot cloud,
	- local state uniquement tampon UI/session,
	- pas de logique metier autoritaire locale divergente.
- Eviter toute duplication de logique de classement:
	- reutiliser les conventions de classement egalitaire de podium (story 19.2) pour les rangs.
- Ne pas introduire de nouvelle librairie pour la normalisation texte: l'API JS native suffit.

### Architecture Compliance

Contraintes derivees des artefacts architecture:
- families/{familyId}/phase est famille-wide et autoritaire.
- Les donnees profil restent scopees par profil.
- Les ecritures cloud doivent rester guardees (auth ready, membership ok, profile hydrate).
- Les pushes cloud doivent conserver le mode anti-boucle/de-dup existant.

### Library/Framework Requirements

- Front: React 18 + Vite 6, TypeScript strict, tests Vitest.
- Cloud: Firebase RTDB via provider existant.
- String comparison: Intl/localeCompare et normalize natifs.

### File Structure Requirements

Fichiers UPDATE probables (a confirmer pendant implementation):
- src/app/App.tsx
- src/app/access-control.ts
- src/types/cloud.ts
- src/services/cloudSyncProvider.ts
- src/hooks/useCloudSync.ts
- src/services/firebase-rtdb.rules.test.ts
- firebase/database.rules.test.json

Fichiers NEW recommandes:
- src/app/destination-survey.ts (logique pure)
- src/app/destination-survey.test.ts
- src/app/App.destination-survey.integration.test.tsx

### UPDATE File Intelligence (current state, change, preserve)

1) src/app/access-control.ts
- Etat actuel: sections autorisees par role/phase, mapping screen->section.
- Changement story: ajouter la nouvelle section/ecran teaser-sondage accessible avant deblocage puis consultable apres deblocage.
- A preserver: restrictions visiteur/utilisateur existantes sur checklist/game/results.

2) src/app/App.tsx
- Etat actuel: rendu principal par phase et screen, push cloud via snapshot, ecrans commentaires lieux deja operationnels.
- Changement story: inserer ecran sondage transforme before/during, saisie 3 propositions max, affichage resultats et points.
- A preserver:
	- flux de deblocage On est partis,
	- hydratation cloud et garde d'acces,
	- navigation existante sans casser bottom nav,
	- logique commentaires lieux (story 21.2).

3) src/types/cloud.ts
- Etat actuel: types cloud checklist/jeu/commentaires.
- Changement story: ajouter type destination survey (reponses profil + metadata timestamp).
- A preserver: compatibilite snapshots anciens via champs optionnels.

4) src/services/cloudSyncProvider.ts
- Etat actuel: parse/push selectif author-only pour commentaires, update payload profile + famille.
- Changement story: parse et push de la branche sondage destination en respectant contraintes role/auteur.
- A preserver: strateges de push partielles, dedup, non-ecrasement des branches non possedees.

5) src/hooks/useCloudSync.ts
- Etat actuel: enveloppe pushSnapshot typed.
- Changement story: propager nouveau champ du sondage dans payload.
- A preserver: queue offline et gardes auth.

6) src/services/firebase-rtdb.rules.test.ts + firebase/database.rules.test.json
- Etat actuel: tests author-only pour placeComments, guards owner-only pour phase.
- Changement story: ajouter regles et tests du sondage (ecriture profil sur sa branche, lecture famille).
- A preserver: couverture des regles existantes sans assouplir owner guards.

### Data Contract Proposal

Proposition de schema RTDB (coherent avec patterns existants):
- families/{familyId}/destinationSurvey/{profileId}
	- profileId
	- proposals: string[] (1..3)
	- updatedAt: number
	- authorUid: string

Regle d'ecriture:
- Un profil ecrit uniquement sa propre entree.
- Ecriture interdite si phase famille = during (fige).

Regle de lecture:
- Membres famille lisent l'ensemble pour affichage resultats.

### Scoring Contract

Definition:
- goodMatch(profile) = au moins une proposition normalisee = destination normalisee.
- basePoints = 20 si goodMatch sinon 0.
- rank sur les seuls profils goodMatch eligibles score partage (role utilisateur), ordre croissant updatedAt.
- bonus: rank 1 => +10, rank 2 => +5, sinon +0.
- totalSurveyPoints = basePoints + bonus.

Egalites:
- Meme timestamp => meme rank et meme bonus.

### Testing Requirements

Unit tests obligatoires:
- normalisation textuelle (casse, accents composes/decomposes, espaces).
- validation max 3 propositions.
- scoring (base, bonus rang 1/2, egalites).
- exclusion score partage pour roles proprietaire/visiteur.

Integration tests obligatoires:
- phase before: edition autorisee.
- phase during: edition bloquee et mode resultat visible.
- affichage qui a vote quoi.
- regression: navigation principale et ecrans existants inchanges.

Rules tests obligatoires:
- author-only write destinationSurvey/{profileId}.
- refus ecriture d'un autre profil.
- refus ecriture si phase= during.

### UX Requirements

L'ecran saisie pre-depart doit afficher clairement:
- "Tu peux proposer jusqu'a 3 destinations."
- "Points: 20 si bonne reponse, +10 si premier, +5 si deuxieme."
- "Apres deblocage, les reponses sont figees et les resultats apparaissent ici."

Ton attendu: familial, ludique, comprensible pour enfants.

## Previous Story Intelligence (25.1)

- 25.1 introduit un ecran teaser avant deblocage avec ton chaleureux et commentaires reutilisables.
- 25.2 doit reutiliser cet ecran (pas creer un ecran parallele concurrent) et preparer naturellement la transformation 25.3.
- Garder la coherence de style avec l'existant mobile-first (header primaire, cartes, CTA clairs).

## Git Intelligence Summary

- Les derniers commits sont concentres sur l'epic 22 (notifications + score progression).
- Pattern dominant: tests d'integration App + utilitaires purs testes.
- Recommandation: suivre le meme pattern pour limiter le risque de regression.

## Latest Tech Information

- JavaScript actuel fournit nativement les primitives utiles:
	- String.prototype.localeCompare avec sensitivity: base pour comparaison insensible a la casse.
	- String.prototype.normalize pour harmoniser formes Unicode equivalentes.
- Aucun besoin d'ajouter une dependance externe pour cette story.

## Dependencies

- Story 25.1 (ecran teaser pre-depart) est prerequise fonctionnellement.
- Story 25.3 depend de la structure de donnees et de l'affichage resultat definis ici.

## Out of Scope

- Sondages sur d'autres themes.
- Configuration dynamique multi-sondages.
- Refonte complete du systeme de score global.

## Completion Status

- Story status: ready-for-dev
- Note: Ultimate context engine analysis completed - comprehensive developer guide created
